name: just_font_scan
description: Scan system font families and their supported weights using platform-native APIs (DirectWrite on Windows).
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  ffi: ^2.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
